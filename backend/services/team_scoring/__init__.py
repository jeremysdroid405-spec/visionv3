"""
Team scoring layer.

Phase 1.A.0 ships the shared `GateAdapter` contract that BOTH player
and team scoring paths can later implement (§4.3 hybrid scoring).
Player-side code is NOT modified in this slice.

Architecture: /app/memory/TEAM_PROPS_ARCHITECTURE.md §4.3 + §11.10.
"""
